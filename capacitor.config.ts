import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sg.tarea3',
  appName: 'Tarea3',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
